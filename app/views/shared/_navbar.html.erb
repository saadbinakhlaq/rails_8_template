<div class="sticky top-0 z-20 backdrop-blur-lg bg-white/30 fixed w-full border-b" data-controller="navbar" data-action="resize@window->navbar#closeOnBigScreen">
  <nav class="p-4 flex justify-between items-center h-20 max-w-7xl mx-auto">
    <%= link_to root_path do %>
      <div class="text-3xl font-extrabold bg-gradient-to-r from-fuchsia-500 to-cyan-500 bg-clip-text text-transparent tracking-wide">Business</div>
    <% end %>
    <div class="flex space-x-6 items-center">
      <div class="hidden md:flex">
        <% if user_signed_in? %>
          <%= button_to "Logout", destroy_user_session_path, method: :delete, class: "px-4 py-2 bg-red-500 text-white font-semibold rounded-lg hover:bg-red-600 transition-colors" %>
        <% end %>
      </div>
      <!-- Mobile Hamburger Menu Icon -->
      <div class="md:hidden text-3xl text-gray-700 cursor-pointer" role="button" data-action="click->navbar#toggle">&#9776;</div>
    </div>
  </nav>

  <!-- Mobile Dropdown Menu -->
  <nav class="absolute top-20 right-0 w-full md:w-1/3 bg-white h-80 overflow-y-auto hidden shadow-lg rounded-lg p-6" data-navbar-target="content">
    <div class="space-y-2">
      <%= (300..350).each do |i| %>
        <p class="p-2 bg-gray-100 hover:bg-gray-200 rounded-md">Item <%= i %></p>
      <% end %>
    </div>
  </nav>
</div>
